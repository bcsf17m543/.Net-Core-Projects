<Window x:Class="Online_Grocery_Store.Views.ReceiptView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Online_Grocery_Store.Views"
        mc:Ignorable="d"
        Title="ReceiptView" Height="450" Width="800">
    <Grid>
        <ListBox x:Name="LB" Margin="0,0,306,161">

        </ListBox>
        <ListView x:Name="ListofItems">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Product Id" Width="120" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Product Name" Width="120" DisplayMemberBinding="{Binding Prodname}" />
                    <GridViewColumn Header="Product Price" Width="120" DisplayMemberBinding="{Binding Price}" />
                    <GridViewColumn Header="Product Quantity" Width="120" DisplayMemberBinding="{Binding Qty}" />

                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel x:Name="mypanel" Margin="500,0,0,0" Orientation="Horizontal">
            <TextBlock Text="Total="></TextBlock>
            <TextBlock x:Name="Ammount" Text="{Binding Total}"></TextBlock>
        </StackPanel>
       
    </Grid>
</Window>
